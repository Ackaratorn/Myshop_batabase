<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Myshop</title>
    <link rel="stylesheet" href="/add.css">
</head>
<body>

    <nav><h1>Myshop</h1></nav>

    <form class="form" action="/product/add" method="POST">
        <h2>เลือกสินค้า</h2>
        <label>French fries (109฿)<input type="number" min="0" value="0" name="french_fries"></label>
        <label>Nugget (115฿)<input type="number" min="0" value="0" name="nugget"></label>

        <h2>ข้อมูลราคา</h2>
        <input type="number" name="cost" placeholder="กรอกราคาต้นทุน" required>
        <input type="number" name="profit" placeholder="กรอกเงินที่ขายได้" required>
        <input type="datetime-local" name="saleTime" required>

        <input type="text" name="description" placeholder="อื่นๆ">
        <button>ยืนยัน</button>
    </form>

    <h2>รายการที่บันทึกแล้ว</h2>
    <table border="1" cellpadding="5" cellspacing="0">
        <tr>
            <th>สินค้า</th>
            <th>จำนวน</th>
            <th>ต้นทุน</th>
            <th>ราคา</th>
            <th>รวม</th>
            <th>เวลา</th>
            <th>อื่นๆ</th>
            <th>จัดการ</th>
        </tr>
        <% products.forEach(product => { %>
        <tr>
            <td>French fries / Nugget</td>
            <td><%= product.french_fries %> / <%= product.nugget %></td>
            <td><%= product.cost %></td>
            <td><%= product.profit %></td>
            <td><%= product.totalPrice %></td>
            <td><%= product.createdAt.toLocaleString() %></td>
            <td><%= product.description %></td>
            <td>
                <a href="/product/edit/<%= product._id %>">แก้ไข</a> |
                <a href="/product/delete/<%= product._id %>" onclick="return confirm('คุณแน่ใจว่าจะลบ?')">ลบ</a>
            </td>
        </tr>
        <% }) %>
    </table>
</body>
</html>
